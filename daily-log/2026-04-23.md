# 2026-04-23 일일 로그

## 오늘 한 일

- 이전에 진행한 전체 기능 확장 작업을 제거하고, 그 작업 전의 단순한 예산 관리 앱 상태로 되돌리는 작업을 시작했다.
- 문서와 설정 파일에서 큰 확장 작업 설명을 제거하고, 월 수입 입력·하루 예산 계산·기본 지출 기록·기본 통계 중심 구조로 복원했다.
- `src/lib`는 localStorage 저장, 하루 예산 계산, 기본 소비 판단, 기본 지출 기록 처리만 남기도록 다시 정리했다.
- `npm.cmd run build`를 다시 실행해 복원된 프로젝트가 빌드되는지 확인했다.
- 앞으로는 GitHub 원격 저장소를 `https://github.com/chun9930/college-budget-web.git`으로 고정하고, 명확한 커밋 메시지와 푸시 규칙을 `AGENTS.md`에 추가했다.
- 현재 단순 기능은 유지한 채, 예전 색감과 카드형 UI 분위기를 다시 입히도록 공통 레이아웃과 각 페이지 마크업을 정리했다.
- 검증: `npm.cmd run build` 성공.
- 특이사항: 현재 워크스페이스에는 이전 확장 작업 결과물이 빠져 있는 상태라, 삭제보다 “이전 단순 구조 재구성” 방식으로 되돌리고 있다.
- 예산 설정 확장 1단계 작업을 시작했다. `storage`에 `budgetSettings`를 추가하고, `budget`에 자동 계산/수동 예산/이관/저축 목표/비상금 제외를 반영하는 `calculateBudgetPlan`을 넣었다.
- `BudgetStatus`는 예산 설정 전용 화면으로 확장했고, `Mainpage`는 실시간 소비 판단에 집중하도록 역할을 분리했다.
- 검증: `node --input-type=module`로 예산 계산과 소비 판정 결과를 확인했고, `npm.cmd run build`가 성공했다.
- 2번 작업으로 이월 규칙을 실제 지출 기록 기반으로 바꿨다. 이제 이월 토글이 켜져 있으면 전날 실제로 남은 예산이 오늘 예산에 자동 반영된다.
- 수동 이월 입력은 제거하고, `ExpenseRecords`의 날짜별 지출 합계를 바탕으로 `calculateBudgetPlan()`이 하루 단위로 이월을 계산한다.
- 검증: `node --input-type=module`로 날짜별 이월 샘플 계산을 확인했고, `npm.cmd run build`가 다시 성공했다.
- 3번 작업으로 경고 단계를 `안정 / 주의 / 경고 / 초과` 4단계로 나눴다. 판단 기준은 `최종 사용 가능 금액` 대비 비율로 고정했고, 같은 계산 함수를 재사용하도록 정리했다.
- 경고 메시지 색상도 상태별로 분리해, 메인 화면에서 바로 위험도를 읽을 수 있게 만들었다.
- 검증: `judgeSpending()` 샘플 입력으로 4단계를 확인했고, `npm.cmd run build`가 다시 성공했다.
- 4번 작업으로 지출 기록을 확장했다. 이제 `결제 수단`, `지출 타입`, `템플릿`을 함께 저장한다.
- `ExpenseRecords`에서 일반 지출 / 고정지출 / 정기지출을 선택할 수 있고, 자주 쓰는 입력은 템플릿으로 저장하거나 불러올 수 있다.
- `Statistics`도 결제 수단별, 지출 타입별 비중을 보여주도록 확장했다.
- 검증: 더미 기록과 템플릿 구조를 새 필드에 맞춰 정리했고, `npm.cmd run build`가 다시 성공했다.

## 다음에 해야할 일

- 브라우저에서 메인, 지출 기록, 통계, 로그인 흐름을 한 번 더 직접 점검한다.
- 이후 변경이 생기면 작업 단위별로 명확한 커밋 메시지와 함께 새 GitHub 저장소에 푸시한다.
- 다음 순서는 `경고 → 기록 → 초과 알림 → 분석`이다.
- 다음 단계에서 이월 규칙을 이용해 경고 강도와 초과 상태를 더 명확히 보여줄 수 있다.
- 다음 단계는 `기록`이며, 같은 계산 함수를 재사용할 수 있도록 지출 데이터 구조를 더 정리하면 된다.
- 다음 단계는 `초과 알림`이며, 오늘/월 기준 초과 상태를 더 눈에 띄게 보여주는 방향으로 확장하면 된다.
