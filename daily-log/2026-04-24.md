# 2026-04-24 일일 로그

## 오늘 한 일

- 5번 작업인 초과 알림 기능을 구현하기 위해 하루/월 초과 기준을 계산과 분리된 알림 상태로 다루는 구조를 정리했다. (커밋: `be06019` / `feat: add over-budget alerts and reset actions`)
- `src/lib/storage.js`에 초과 알림 확인 상태를 저장하는 키와 헬퍼를 추가했고, `window`가 없는 검증 환경에서도 안전하게 동작하도록 localStorage 접근을 방어했다.
- `src/lib/budget.js`에 하루 초과와 월 초과를 각각 판정하는 공통 알림 계산 함수를 추가했다.
- `Mainpage`와 `BudgetStatus` 화면에 초과 배너와 재설정 버튼을 붙여, 초과 상황을 바로 확인하고 알림만 다시 계산할 수 있게 했다.
- 예산 숫자를 바꾸지 않고 초과 알림 상태만 초기화하는 방식으로 리스크를 줄였다.
- `README.md`에 초과 알림과 재설정 규칙을 반영했다.
- 검증: `npm.cmd run build` 성공.
- 검증: Node에서 하루/월 초과 판정이 분리되는지 확인했고, 하루 초과와 월 초과가 함께 계산되는 경우도 정상 확인했다.
- 특이사항: `storage.js`의 localStorage 접근이 Node 환경에서 `window` 없이도 안전하게 동작하도록 수정했다.
- 6번 작업으로 통계 화면을 보강했다. 최근 3개월 지출 추세, 카테고리 초과 감지, 절약 힌트가 보이도록 계산 함수를 `src/lib`에 추가하고 `Statistics`를 다시 구성했다. (커밋: `2ca7a37` / `feat: enhance expense analysis views`)
- 데이터가 없을 때도 화면이 깨지지 않도록 빈 상태 문구를 유지했고, 복잡한 그래프 대신 카드형 요약과 단순 막대만 사용했다.
- 검증: 통계 보강용 계산 함수 샘플 데이터를 Node에서 확인했고, `npm.cmd run build`가 다시 성공했다.

## 다음에 해야할 일

- 브라우저에서 `#/`와 `#/budget-status`를 열어 초과 배너와 재설정 버튼이 실제로 보이는지 확인한다.
- 브라우저에서 `#/statistics`를 열어 최근 3개월 추세, 카테고리 초과 감지, 절약 힌트가 직관적으로 보이는지 확인한다.
- 변경사항이 더 생기면 명확한 커밋 메시지로 새 GitHub 저장소에 계속 push한다.
